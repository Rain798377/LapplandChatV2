services:
  lappland:
    build: .
    environment:
      GROQ_API_KEY: REPLACE_WITH_YOUR_GROQ_API_KEY
      DISCORD_TOKEN: REPLACE_WITH_YOUR_DISCORD_TOKEN
    container_name: lappland
    restart: unless-stopped
    volumes:
      - ./app:/app
      - ./data:/app/data